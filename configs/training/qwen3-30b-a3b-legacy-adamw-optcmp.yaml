extends: qwen3-30b-a3b-legacy.yaml

experiment:
  name: qwen3-30b-a3b-legacy-adamw-optcmp
  description: Legacy CoT LoRA with the tuned AdamW baseline

paths:
  adapter_dir: outputs/adapters/qwen3-30b-a3b-legacy-adamw-optcmp

optimizer:
  name: adamw_torch_fused
  kwargs: {}

training:
  output_dir: outputs/trainer/qwen3-30b-a3b-legacy-adamw-optcmp
  learning_rate: 2.0e-4
  weight_decay: 0.0
  lr_scheduler_kwargs:
    min_lr: 1.0e-5
